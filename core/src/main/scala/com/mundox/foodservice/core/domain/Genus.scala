package com.mundox.foodservice.core.domain

trait Genus

object Genus {
  case class Actinidia() extends Genus
  case class Ananas() extends Genus
  case class Apteryx() extends Genus
  case class Artocarpus() extends Genus
  case class Cactaceae() extends Genus
  case class Carica() extends Genus
  case class Citrullus() extends Genus
  case class Citrus() extends Genus
  case class Cucumis() extends Genus
  case class Diospyros() extends Genus
  case class Durio() extends Genus
  case class Ficus() extends Genus
  case class Fragaria() extends Genus
  case class Litchi() extends Genus
  case class Malus() extends Genus
  case class Mangifera() extends Genus
  case class Morus() extends Genus
  case class Musa() extends Genus
  case class Passiflora() extends Genus
  case class Persea() extends Genus
  case class Prunus() extends Genus
  case class Psidium() extends Genus
  case class Punica() extends Genus
  case class Pyrus() extends Genus
  case class Selenicereus() extends Genus
  case class Sellowiana() extends Genus
  case class Solanum() extends Genus
  case class Ribes() extends Genus
  case class Rubus() extends Genus
  case class Vaccinium() extends Genus
  case class Vitis() extends Genus
}